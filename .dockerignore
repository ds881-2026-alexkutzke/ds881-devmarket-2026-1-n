# Git / VCS
.git
.gitignore

# Editor / IDE
.vscode
.idea

# Dependências e builds (instalados/gerados dentro da imagem)
node_modules
dist
build
coverage

# Variáveis de ambiente — nunca vão pra imagem
.env
.env.*

# Docker (a própria imagem não precisa)
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.yaml

# CI / configs externas ao runtime
.github
.tool-versions

# Docs
*.md
